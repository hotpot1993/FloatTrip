# 途见 · FloatTrip

对话式旅行规划：先通过对话收集并确认旅行需求，再由多 Agent 生成可打开、可编辑、可追溯的行程。

本文件只定义这个上下文里**特有**的术语。它是一份词汇表，不是规格、不是设计稿、不记录实现细节。

## 行程与版本

**行程（Itinerary）**:
一次规划产出的、可独立打开与编辑的一版完整路线内容。它是历史行程列表里的一张卡，也是删除操作的作用单位。
_Avoid_: 计划（plan）、方案、trip、行程行

**行程版本链（Lineage）**:
同一趟旅行经「继续修改」派生出的多版行程。修改产生**新的**行程而不是覆盖原件，链上每版指回它的派生来源，版本号递增。
_Avoid_: 历史版本、修订记录、版本历史

**一趟旅行（Trip）**:
一条行程版本链所代表的同一次出行，由链的公共源头界定，携带共同的目的地、日期范围与天数。
_Avoid_: 订单、出游记录、行程（当你想说的是"整趟出行"时）

**历史行程（Itinerary archive）**:
某用户全部行程版本的扁平列表，按创建时间倒序。它**不是**「一趟旅行」的列表——同一趟旅行的多个版本各占一张卡，并各自标注为修改版。
_Avoid_: 行程库、我的旅行、历史记录

## 到达与返程

**抵达时刻（Arrival time）**:
用户本次行程到达目的地、可以开始活动的时间。它是**行程**范围内的结构化事实，随需求单提交被冻结；它不属于用户画像，跨行程不继承。
_Avoid_: 到达时间、落地时间、作息偏好

**返程时刻（Departure time）**:
用户本次行程离开目的地的时间，同样是行程范围内的结构化事实。行程末日的活动必须在它之前留出赶车缓冲。
_Avoid_: 离开时间、出发时间、结束时间

**抵达可用下界（Arrival floor）**:
当用户的抵达表述不够精确时，系统为排程与核验采用的保守时间下界。它只约束「不得早于何时开始安排」，不代表系统知道了用户的真实抵达时刻。
_Avoid_: 归一化抵达时间、默认抵达时间、实际抵达时间

## 规划过程

**规划需求单（Planning Brief）**:
在对话中逐步收集、由用户显式确认后才启动正式规划的旅行需求。未确认前始终可修改；缺少目的地或有效日期范围时不可提交。
_Avoid_: 需求、表单、brief 数据

**规划任务（Planning Run）**:
一次正式规划或修改的执行记录，携带提交时冻结、之后不可变的请求快照，拥有独立的生命周期（排队、执行、等待用户、成功、失败、取消）。
_Avoid_: 任务、job、行程（规划任务不是行程）

**字段提问（Field question）**:
服务端从固定提问清单中算出的、当前唯一待回答的那一项。它对应规划需求单里的一个字段或字段组，携带候选项，是对话里唯一可以直接写入需求单的封闭选择；被回答或跳过后才推进到下一项。
_Avoid_: 追问、澄清、表单题、问题列表

**澄清提问（Clarification question）**:
系统在目标不唯一或确实无法理解时提出的问题。它不对应任何字段，因此回答必须回到普通对话，由对话理解决定其含义。
_Avoid_: 追问、字段提问、报错

**已跳过字段（Declined field）**:
用户对某个可选项明确表示「还不确定」或「没有特别要求」的记录。它只表示这一项已经被问过并处理完，不代表系统采用了任何取值，也不影响需求单能否提交。
_Avoid_: 空值、默认值、忽略项、未填写

**收集已完成（Collection complete）**:
提问清单的每一项都已被回答或已被跳过的状态。它是决定界面何时展示需求摘要的唯一依据，与需求单能否提交是两件独立的事——需求单可以在收集完成之前就进入可提交状态。
_Avoid_: 完成、信息齐全、ready、需求完整

## 外部数据额度

**配额池（Quota pool）**:
高德按「服务组」分配的一份共享月额度的计量范围。同池内的所有接口、同一账号下的所有 Key 合计消耗同一份额度；池与池之间的用量互不抵消。
_Avoid_: 接口额度、Key 额度、应用额度、分组

**已用量（Quota usage）**:
系统对某个配额池在当前自然月内已消耗次数的本地记录。它是**估算**而不是对账结果：计数范围只覆盖本服务发出的请求，可以由人工录入补正，但无法自动与高德控制台同步。
_Avoid_: 消耗量、剩余额度、真实用量、余额

**用量对账（Usage reconciliation）**:
把从高德控制台读到的已用数值录入系统，以补正本地计数无法知道的部署前消耗。它设置的是绝对值，且不覆盖此后本地继续累加的部分。
_Avoid_: 校准、同步、重置用量
